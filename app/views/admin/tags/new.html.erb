<div id="headingbg"><h2>Add a Tag</h2></div>
<div id="inner">
<% form_for([:admin, @tag]) do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit "Create Tag" %>
  </p>
<% end %>
</div>
<%= link_to 'Back', admin_tags_path %>