<div id="headingbg"><h2>Add a MetaTag</h2></div>
<div id="inner">
<% form_for([:admin, @metatag]) do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit "Create MetaTag" %>
  </p>
<% end %>
</div>
<%= link_to 'Back', admin_metatags_path %>