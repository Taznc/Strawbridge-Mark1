<h1 style="color: black;">New comment</h1>

<%= error_messages_for :comment %>

<%= render :partial => @comment, 
  :locals => { :button_name => "Add Comment"} %>

<%= link_to 'Back', article_comments_path(@article) %>