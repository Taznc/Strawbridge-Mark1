<% for category in these_categories %>
	<p style="color: #E44E01; margin:0; padding: 0;"><%= link_to category.name, category_path(category) %>
	(<%= link_to category.articles.size, category_path(category) %>)
	</p>
<% end %>